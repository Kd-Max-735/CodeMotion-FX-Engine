# 分形 `fractal`

## 1. 工具作用

使用 Mandelbrot 逃逸时间算法生成确定性二维分形采样，可控制观察中心、缩放、旋转、迭代精度和动画速度。

## 2. 输出要求

模型只输出 `{ "type": "fractal", "data": { ... } }` JSON。顶层不得有额外字段，`data` 不得有未声明字段，也不要输出说明文字、资源标识或文件位置。

## 3. 合法 JSON 示例

```json
{"type":"fractal","data":{"gridSize":48,"iterations":160,"centerX":-0.745,"centerY":0.11,"zoom":35,"rotation":0,"speed":0,"insideColor":"#081C15","outsideColor":"#D8F3DC"}}
```

## 4. 参数表

| 参数 | 类型 | 范围/选项 | 默认值 | 选择策略 |
| --- | --- | --- | --- | --- |
| `gridSize` | 整数 | 8–64 | 32 | 控制采样网格精度 |
| `iterations` | 整数 | 16–256 | 80 | 控制边界细节；与网格共同影响计算量 |
| `centerX` | 数值 | -2.5–1 | -0.5 | 水平观察中心 |
| `centerY` | 数值 | -1.5–1.5 | 0 | 垂直观察中心 |
| `zoom` | 数值 | 0.25–200 | 1 | 越大越深入局部细节 |
| `rotation` | 数值 | -180–180 度 | 0 | 静态旋转视野 |
| `speed` | 数值 | -2–2 | 0.18 | 旋转动画速度，负值反向，设为 0 时静止 |
| `insideColor` | `#RRGGBB` | 六位十六进制 | `#081C15` | 集合内部颜色 |
| `outsideColor` | `#RRGGBB` | 六位十六进制 | `#D8F3DC` | 逃逸区域颜色 |

`gridSize² × iterations` 不得超过 1048576；当前字段范围恰好把绝对上限限制在该预算内。

## 5. 表达映射

“复杂/精细”提高 `iterations`，必要时提高 `gridSize`；“放大某处”提高 `zoom` 并用 `centerX/centerY` 对准位置；“旋转/反转”设置 `rotation` 或 `speed`；颜色要求只转换成两个直接色值。

## 6. 自然语言例子

- “经典 Mandelbrot 全景” → 默认中心、`zoom` 1、`rotation` 0。
- “深入海马谷并保持静止” → 中心约 (-0.745, 0.11)、高 `zoom`、`speed` 0。
- “缓慢顺时针旋转的分形” → 小幅正 `speed`。
- “逆时针快速旋转” → 负 `speed`，绝对值约 1–2。
- “边缘更细致” → 提高 `iterations`，再酌情提高 `gridSize`。
- “深紫与橙色高反差” → 设置 `insideColor` 和 `outsideColor`，不引用调色板。

## 7. 推荐档位

| 档位 | `gridSize` | `iterations` | `zoom` |
| --- | ---: | ---: | ---: |
| 概览 | 16–24 | 32–64 | 0.5–3 |
| 均衡 | 32–48 | 80–144 | 1–40 |
| 深入 | 48–64 | 160–256 | 20–200 |

## 8. 默认值与中性行为

默认值显示经典 Mandelbrot 视野并保持轻微连续旋转，以中等迭代和网格取得稳定的质量/成本平衡；明确要求静止时把 `speed` 设为 0。

## 9. 不能处理的内容

服务器可选绑定一张 `background_image` 图片作为分形底图，图片身份始终与模型参数分离。不能生成 Julia 集自定义常数、三维分形、外部调色板、文件路径、URL、资源 ID、时间线或其他工具调用。
